<UserControl x:Class="Win11IsoBuilder.Views.OfficeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <CheckBox Content="Install Microsoft 365 Apps (offline)" IsChecked="{Binding OfficeEnabled}"
                  FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock TextWrapping="Wrap" Foreground="#6B7280" Margin="0,0,0,12"
                   Text="Microsoft 365 Apps for business · en-us · 64-bit · Monthly Enterprise. Source (~3.5 GB) is downloaded once and cached. Activation is done by the user after install (no key is embedded)." />

        <TextBlock Text="Exclude apps" FontWeight="SemiBold" Margin="0,0,0,4"
                   IsEnabled="{Binding OfficeEnabled}" />
        <ItemsControl ItemsSource="{Binding ExcludableApps}" IsEnabled="{Binding OfficeEnabled}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding DisplayName}" IsChecked="{Binding IsExcluded}"
                              Width="200" Margin="0,0,8,4" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
